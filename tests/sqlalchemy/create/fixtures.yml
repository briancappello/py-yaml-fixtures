Parent:
  p1:
    name: First Parent

Child:
  c1:
    name: First Child
    parent: Parent(p1)
    node: Node(root)

Node:
  root:
    name: Root

  left:
    name: Left
    root: Node(root)

  right:
    name: Right
    root: Node(root)

  left_leaf:
    name: Left Leaf
    root: Node(left)

  right_leaf:
    name: Right Leaf
    root: Node(right)
